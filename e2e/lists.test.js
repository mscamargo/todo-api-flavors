import { describe, it } from 'node:test'

describe('[POST] /lists', () => {
  it.todo('should create a list correctly')
  it.todo('should return a validation error for an invalid a to-do item')
})

describe('[GET] /lists', () => {
  it.todo('should return a list of lists')
})

describe('[GET] /lists/:id', () => {
  it.todo('should return a detailed list')
})

describe('[PUT] /lists/:id', () => {
  it.todo('should return a validation error for an invalid data')
  it.todo('should successfully update a list')
})

describe('[DELETE] /lists/:id', () => {
  it.todo('should delete a list')
})
